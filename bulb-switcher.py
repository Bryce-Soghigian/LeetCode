class Solution:
    def bulbSwitch(self, n: int) -> int:
        # We to build an n by n matrix and for each round we only turn off the ones on the right
        return math.floor(math.sqrt(n))
        
        """
        
        
        
        
        """